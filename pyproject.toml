[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "badness-pre-commit"
version = "0.9.0"
description = "A pre-commit hook for badness"
requires-python = ">=3.10"
dependencies = ["badness==0.9.0"]

[tool.setuptools]
py-modules = []
