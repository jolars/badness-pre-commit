[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "badness-pre-commit"
version = "0.12.0"
description = "pre-commit hook wrapper for badness"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = ["badness==0.12.0"]

[tool.setuptools]
py-modules = []
